本地函数状态
=====================

这是本地函数特性当前及待完成工作的检查清单，规范详见 [local-functions.md](./local-functions.md)。

--------------------

已知问题
============

编译器：

- [ ] 未启用该特性时解析器仍构建本地函数节点（#9940）
- [ ] 编译器崩溃：在状态机方法中对状态机方法进行基调用（#9872）
- [ ] 需要为未使用的本地函数添加自定义警告（#9661）
- [ ] 快速操作的生成不提供生成本地函数的选项（#9352）
- [ ] 解析器二义性研究（#10388）
- [ ] Dynamic 支持（#10389）
- [ ] 在表达式树中引用本地函数（#10390）
- [ ] 解决明确赋值规则（#10391）
- [ ] 移除对 `var` 返回类型的支持（#10392）
- [ ] 更新错误消息（#10393）

IDE：

- [ ] 某些围绕本地函数的选择导致提取方法重构失败（#8719）
- [ ] 提取传递给 Action 的本地函数会引入编译器错误（#8718）
- [ ] 在委托调用上按 Ctrl+. 导致 VS 崩溃（通过提取方法）（#8717）
- [ ] 内联临时变量引入编译器错误（#8716）
- [ ] 调用层次结构搜索在本地函数上永不终止（#8654）
- [ ] 导航栏不支持本地函数（#8648）
- [ ] 本地函数没有大纲显示（#8647）
- [ ] 使用不支持的修饰符时到处出现红色波浪线（#8645）
- [ ] 查看定义对本地函数报错（#8644）
- [ ] 声明本地函数时不推荐 void 关键字（#8616）
- [ ] 更改签名不更新本地函数的签名（#8539）


特性完成进度
=============================

- [x] N 级嵌套本地函数
- [x] 捕获
    - 与 lambda 一起工作，在回退情况下行为非常相似
    - 对于从不转换为委托且不是迭代器/async 的函数，具有零分配闭包（按引用传递的结构体帧）
- [x] 标准参数特性
    - params
    - ref/out
    - 命名/可选
- [x] 可见性
    - 可能重新审视设计（当前为遮蔽，可能改为重载）
- [x] 泛型
    - 泛型方法中的非泛型本地函数（与 lambda 相同）
    - 非泛型方法中的泛型本地函数
    - 泛型方法中的泛型本地函数
    - 泛型本地函数的任意嵌套
    - 带约束的泛型本地函数
- [x] 推断返回类型
- [x] 迭代器
- [x] Async
