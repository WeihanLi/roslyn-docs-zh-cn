# C# 8 模式匹配扩展的未解决问题

### 规范
- [ ] 需要用添加的语法形式更新规范。有关 C# 7 的拟议更改摘要，请参见 https://github.com/dotnet/csharplang/issues/1054。

### 未实现部分
- [ ] 当使用通配符但作用域内存在名为 `_` 的内容时，给出错误。
- [ ] 在递归模式中实现 switch 语句
  - [x] 解析
  - [x] 绑定
  - [ ] 降级
  - [ ] 代码生成
  - [ ] 调试信息
  - [ ] 编辑并继续
- [ ] 实现 match 表达式
  - [ ] 解析
  - [ ] 绑定
  - [ ] 降级
  - [ ] 代码生成

- [ ] 如果 `var` 在 var 模式中绑定到某个类型，应报错。实现并测试。

### 需要测试计划
- [ ] 需要为这些新增内容制定测试计划。以下是一些需要的测试的高层次列表
  - [ ] 递归模式中模式变量的作用域机制是否正常工作
  - [ ] 在脚本中，递归模式中的顶层模式变量变为字段
  - [ ] 需要识别并测试 IDE 场景
  - [ ] 模糊测试生成器应扩展以支持递归模式
  - [ ] 测试属性模式中应支持的各种符号
  - [ ] 测试在属性模式中不合法命名属性的所有方式
  - [ ] 需要为编辑并继续中模式临时变量的数据表示设计方案
  - [ ] 需要按编辑并继续规范精确共享临时变量
  - [ ] 此处需要针对原型注释的要点或 GitHub issue

- [ ] 需要确保良好的代码质量，例如避免在类型测试前进行冗余的 null 检查
- [ ] 需要确保决策树大小爆炸与冗余测试执行之间的良好权衡
