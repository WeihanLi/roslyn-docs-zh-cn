# 内部可访问性

C# 语言规范在描述内部可见性时非常模糊。我们将"程序"映射到正在编译的程序集；`internal` 可访问性不会扩展到导入编译的程序集，除非通过使用 `InternalsVisibleToAttribute` 专门授予该访问权限。

有许多地方这会影响语言的语义，而 C# 语言规范没有相应的支持性语言。例如，如果当前程序集中存在与另一个程序集中相同名称冲突的类型名称，我们允许对该名称的引用并将其视为对当前程序集中该名称的引用。此外，如果冲突的类型来自另一个程序集，则不强制执行 using 别名不得与同名类型冲突的规则。
