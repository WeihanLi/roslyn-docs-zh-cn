全局命名空间中的扩展方法
=========================================

如果您创建了一个编译，其中导入的库被分配了一个别名（而不是默认的 "global" 别名），Roslyn 编译器不会自动看到该库中（未命名的顶级）全局命名空间中的扩展方法。您必须使用别名在代码中导入该库，才能将这些方法视为扩展方法。

以前版本的编译器会将此类方法视为扩展方法，尽管它们没有被导入。

另请参阅 https://github.com/dotnet/roslyn/issues/1079。
